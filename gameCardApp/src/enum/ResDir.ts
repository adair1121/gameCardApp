let RES_RESOURCE = `resource/`;

let COMMON_DIR = `resource/res/common/`;

let EFFECT = `resource/res/animate/`;

let ITEM = `resource/res/item/`;

let MAP_DIR=`${RES_RESOURCE}map/`;

let CFG_DIR = `${RES_RESOURCE}config/`;

let SKILL_EFF = `${EFFECT}skill/`;

let EFFROLESHOW = `${EFFECT}show/`;

let SOLDIER = `${EFFECT}soldier/`

let DEFAULT_FONT = `${RES_RESOURCE}res/font/hwkt.ttf`
